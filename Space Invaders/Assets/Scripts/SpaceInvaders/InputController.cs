using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InputController : MonoBehaviour {

	public void GetInput(string input)
    {      
        //new Database().addUser(new User(input)); //Creamos un usuario a partir del nombre que introduce, al terminar la partida utilizaremos
        //el setPoints de la clase User
    }
}
